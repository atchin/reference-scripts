# install.packages("tidyverse")
install.packages("here")
library(here)
## Kenzie/Dan data
# install.packages("tidyverse")
library(tidyverse, here)
library(here)
here:here()
here()
data <- read_csv(here("./data/Kitsap_KenzieDan.csv")) # tells R where the data is located;
str(data)
glimpse(data)
## identify NFB and NA sites
data2 <- data %>%
mutate(NFB_logi = str_detect(c(Title, Description), 'NFB'), # see if this works
nocrossing_logi = str_detect(c(Title, Description), 'No Site'|'Nothing')) %>%
drop_na(NFB_logi) # remove actual NA rows since it screws up the if-else loop
## identify NFB and NA sites
data2 <- data %>%
mutate(NFB_logi = str_detect(c(Title), 'NFB'), # see if this works
nocrossing_logi = str_detect(c(Title, Description), 'No Site'|'Nothing')) %>%
drop_na(NFB_logi) # remove actual NA rows since it screws up the if-else loop
## identify NFB and NA sites
data2 <- data %>%
mutate(NFB_logi = str_detect(Title, 'NFB'), # see if this works
nocrossing_logi = str_detect(c(Title, Description), 'No Site'|'Nothing')) %>%
drop_na(NFB_logi) # remove actual NA rows since it screws up the if-else loop
## identify NFB and NA sites
data2 <- data %>%
mutate(NFB_logi = str_detect(Title, 'NFB'), # see if this works
nocrossing_logi = str_detect(c(Title, Description), 'No Site|Nothing')) %>%
drop_na(NFB_logi) # remove actual NA rows since it screws up the if-else loop
## identify NFB and NA sites
data2 <- data %>%
mutate(NFB_logi = str_detect(Title, 'NFB'),
nocrossing_logi = str_detect(Title, 'No Site|Nothing')) %>%
drop_na(NFB_logi) # remove actual NA rows since it screws up the if-else loop
glimpse(data2) # check
View(data2) # ok but really check for real this time
data <- read_csv(here("./data/Kitsap_KenzieDan.csv")) # tells R where the data is located;
str(data)
glimpse(data)
## identify NFB and NA sites
data2 <- data %>%
mutate(NFB_logi = str_detect(Title, 'NFB'),
nocrossing_logi = str_detect(Title, 'No Site|Nothing')) %>%
drop_na(NFB_logi) # remove actual NA rows since it screws up the if-else loop
glimpse(data2) # check
View(data2) # ok but really check for real this time
